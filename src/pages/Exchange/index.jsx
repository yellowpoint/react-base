import { Button, Space } from 'antd-mobile';
import { useNavigate } from 'react-router-dom';

// import styles from './index.module.less';

const Exchange = () => {
  const navigate = useNavigate();
  return <Space wrap>Exchange</Space>;
};

export default Exchange;
