import { Button, Space } from 'antd-mobile';
import { useNavigate } from 'react-router-dom';

// import styles from './index.module.less';

const Collection = () => {
  const navigate = useNavigate();
  return <Space wrap>Collection</Space>;
};

export default Collection;
