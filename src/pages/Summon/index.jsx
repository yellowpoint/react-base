import { Button, Space } from 'antd-mobile';
import { useNavigate } from 'react-router-dom';

// import styles from './index.module.less';

const Summon = () => {
  const navigate = useNavigate();
  return <Space wrap>Summon</Space>;
};

export default Summon;
